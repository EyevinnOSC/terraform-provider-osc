---
page_title: "eyevinn-openauth-pwd: OpenAuth Password"
subcategory: "Services"
description: |-
  Boost your cybersecurity with OpenAuth Password Service! This ready-to-deploy solution empowers your authentication processes using a reliable CouchDB database and seamless email verification for i...
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# OpenAuth Password (`eyevinn-openauth-pwd`)

Boost your cybersecurity with OpenAuth Password Service! This ready-to-deploy solution empowers your authentication processes using a reliable CouchDB database and seamless email verification for impervious ID security.

[Service documentation](https://docs.osaas.io/osaas.wiki/Service%3A-OpenAuth-Password.html) · [Source](https://github.com/Eyevinn/openauth-pwd)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "eyevinn-openauth-pwd"
  name       = "mypwd"

  parameters = {
    UserDbUrl     = "..."
    SmtpMailerUrl = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `UserDbUrl` | string | yes | Database connection configuration |
| `SmtpMailerUrl` | string | yes | URL endpoint for external service |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
