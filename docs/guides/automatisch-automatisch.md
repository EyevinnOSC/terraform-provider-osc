---
page_title: "automatisch-automatisch: Automatisch"
subcategory: "Services"
description: |-
  Transform your business with Automatisch, the open-source automation tool that seamlessly connects apps like Twitter, Slack, and more.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# Automatisch (`automatisch-automatisch`)

Transform your business with Automatisch, the open-source automation tool that seamlessly connects apps like Twitter, Slack, and more. Enhance efficiency and maintain data control with ease and flexibility.

[Service documentation](https://docs.osaas.io/osaas.wiki/Service%3A-Automatisch.html) · [Source](https://github.com/automatisch/automatisch)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "automatisch-automatisch"
  name       = "myautomatisch"

  parameters = {
    RedisUrl    = "..."
    PostgresUrl = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `RedisUrl` | string | yes | URL endpoint for external service |
| `PostgresUrl` | string | yes | URL endpoint for external service |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
