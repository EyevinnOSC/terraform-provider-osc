---
page_title: "boldare-openai-assistant: OpenAI Assistant"
subcategory: "Services"
description: |-
  Transform your NestJS application with our AI Assistant library, offering fast setup and seamless integration with OpenAI for dynamic conversational experiences.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# OpenAI Assistant (`boldare-openai-assistant`)

Transform your NestJS application with our AI Assistant library, offering fast setup and seamless integration with OpenAI for dynamic conversational experiences. Develop efficient chatbots in minutes!

[Source](https://github.com/boldare/openai-assistant)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "boldare-openai-assistant"
  name       = "myassistant"

  parameters = {
    OpenAiApiKey = "..."
    AssistantId  = "..."
    # AppUrl       = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `OpenAiApiKey` | string | yes | Enter Open AI API key |
| `AssistantId` | string | yes |  |
| `AppUrl` | string | no | For embedding the assistant in your website |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
