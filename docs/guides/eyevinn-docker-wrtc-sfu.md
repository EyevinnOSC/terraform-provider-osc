---
page_title: "eyevinn-docker-wrtc-sfu: Symphony Media Bridge"
subcategory: "Services"
description: |-
  Elevate your broadcast streaming with docker-wrtc-sfu: a seamless SFU solution, harnessing Symphony Media Bridge in a Docker container.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# Symphony Media Bridge (`eyevinn-docker-wrtc-sfu`)

Elevate your broadcast streaming with docker-wrtc-sfu: a seamless SFU solution, harnessing Symphony Media Bridge in a Docker container. Achieve unparalleled WebRTC performance and flexibility effortlessly.

[Service documentation](https://docs.osaas.io/osaas.wiki/Service%3A-Symphony-Media-Bridge-WHIP-Gateway.html) · [Source](https://github.com/Eyevinn/docker-wrtc-sfu)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "eyevinn-docker-wrtc-sfu"
  name       = "mysfu"

  sensitive_parameters = {
    ApiKey = var.api_key
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `ApiKey` | string | yes | Choose a key to use for access to the API Sensitive, set it in `sensitive_parameters`. |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
