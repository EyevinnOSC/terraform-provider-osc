---
page_title: "valkey-io-valkey: valkey"
subcategory: "Services"
description: |-
  Introducing Valkey: a Redis-compatible high-performance key-value store with wide range support.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# valkey (`valkey-io-valkey`)

Introducing Valkey: a Redis-compatible high-performance key-value store with wide range support. Build on various systems, extensible plugin system, and TLS support available.

[Service documentation](https://docs.osaas.io/osaas.wiki/Service%3A-Valkey.html) · [Source](https://github.com/valkey-io/valkey)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "valkey-io-valkey"
  name       = "myvalkey"

  parameters = {
    # Password = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `Password` | string | no | Sets the authentication password for connecting to the Valkey server. This password would be used by clients to authenticate when the server has authentication enabled. |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
