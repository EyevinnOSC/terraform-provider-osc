---
page_title: "neo4j-docker-neo4j: Neo4j"
subcategory: "Services"
description: |-
  Harness the power of connected data with Neo4j's easy-to-deploy Docker images! Perfect for both developers and enterprises, Neo4j offers swift setup and data persistence for insightful analytics an...
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# Neo4j (`neo4j-docker-neo4j`)

Harness the power of connected data with Neo4j's easy-to-deploy Docker images! Perfect for both developers and enterprises, Neo4j offers swift setup and data persistence for insightful analytics and graph performance.

[Source](https://github.com/neo4j/docker-neo4j)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "neo4j-docker-neo4j"
  name       = "myneo4j"

  parameters = {
    # Auth = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `Auth` | string | no | Sets the authentication credentials for Neo4j database access. This environment variable typically configures the username and password combination for database authentication. |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
