---
page_title: "birme-bucket-commander: Bucket Commander"
subcategory: "Services"
description: |-
  Manage your S3 buckets effortlessly with Bucket Commander, offering a Norton Commander-inspired dual-pane interface.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# Bucket Commander (`birme-bucket-commander`)

Manage your S3 buckets effortlessly with Bucket Commander, offering a Norton Commander-inspired dual-pane interface. Experience seamless navigation, secure credential management, and quick file operations.

[Source](https://github.com/birme/bucket-commander)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "birme-bucket-commander"
  name       = "mycommander"

  sensitive_parameters = {
    OscAccessToken = var.osc_access_token
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `OscAccessToken` | string | yes | Access token for Open Source Cloud services, required for S3-to-S3 file copy operations with real-time job monitoring Sensitive, set it in `sensitive_parameters`. |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
