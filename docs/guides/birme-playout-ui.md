---
page_title: "birme-playout-ui: Playout UI"
subcategory: "Services"
description: |-
  Elevate your media scheduling with Playout UI! Seamlessly manage playlists with live time display, real-time progress tracking, and backend flexibility.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# Playout UI (`birme-playout-ui`)

Elevate your media scheduling with Playout UI! Seamlessly manage playlists with live time display, real-time progress tracking, and backend flexibility. Effortlessly organize, edit, and control playback. Ideal for dynamic environments!

[Source](https://github.com/birme/playout-ui)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "birme-playout-ui"
  name       = "myui"

  parameters = {
    DbUrl       = "..."
    Username    = "admin"
    Password    = "..."
    # Database    = "..."
    # CorsOrigins = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `DbUrl` | string | yes |  |
| `Database` | string | no |  |
| `Username` | string | yes | Default `admin`. |
| `Password` | string | yes |  |
| `CorsOrigins` | string | no |  |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
