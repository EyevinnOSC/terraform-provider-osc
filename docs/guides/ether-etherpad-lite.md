---
page_title: "ether-etherpad-lite: Etherpad Lite"
subcategory: "Services"
description: |-
  Unleash seamless collaboration with Etherpad, the ultimate real-time web editor! Host unlimited users on your servers, secure data control, and customize with essential plugins.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# Etherpad Lite (`ether-etherpad-lite`)

Unleash seamless collaboration with Etherpad, the ultimate real-time web editor! Host unlimited users on your servers, secure data control, and customize with essential plugins. Elevate teamwork today!

[Source](https://github.com/ether/etherpad-lite)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "ether-etherpad-lite"
  name       = "mylite"

  parameters = {
    # DatabaseUrl = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `DatabaseUrl` | string | no | Specifies the database connection URL for Etherpad. This allows you to connect to an external database instead of using the default dirtyDB driver. |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
