---
page_title: "knadh-listmonk: Listmonk"
subcategory: "Services"
description: |-
  Elevate your email marketing with listmonk! Fast, feature-packed self-hosted newsletter and mailing list manager in a single binary, backed by PostgreSQL.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# Listmonk (`knadh-listmonk`)

Elevate your email marketing with listmonk! Fast, feature-packed self-hosted newsletter and mailing list manager in a single binary, backed by PostgreSQL. Perfect for seamless campaigns and data control.

[Source](https://github.com/knadh/listmonk)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "knadh-listmonk"
  name       = "mylistmonk"

  parameters = {
    DatabaseUrl = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `DatabaseUrl` | string | yes | PostgreSQL database connection URL for listmonk's data store. This is the primary database where all subscriber lists, campaigns, templates, and application data are stored. |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
