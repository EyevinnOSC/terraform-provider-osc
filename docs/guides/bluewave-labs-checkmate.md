---
page_title: "bluewave-labs-checkmate: Checkmate"
subcategory: "Services"
description: |-
  Monitor servers effortlessly with Checkmate—a powerful open-source tool for tracking server and website performance.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# Checkmate (`bluewave-labs-checkmate`)

Monitor servers effortlessly with Checkmate—a powerful open-source tool for tracking server and website performance. Enjoy real-time alerts, in-depth insights, and manage over 1000 servers seamlessly!

[Source](https://github.com/bluewave-labs/checkmate)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "bluewave-labs-checkmate"
  name       = "mycheckmate"

  parameters = {
    # SystemEmailHost     = "..."
    # SystemEmailPort     = "..."
    # SystemEmailAddress  = "..."
    # SystemEmailPassword = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `SystemEmailHost` | string | no |  |
| `SystemEmailPort` | string | no |  |
| `SystemEmailAddress` | string | no |  |
| `SystemEmailPassword` | string | no |  |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
