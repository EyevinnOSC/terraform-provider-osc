---
page_title: "usefathom-fathom: Fathom Lite"
subcategory: "Services"
description: |-
  Introducing Fathom Lite - the popular, open-source website analytics tool with millions of downloads! Long-term maintenance, bug fixes, and cookie-free tracking set it apart.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# Fathom Lite (`usefathom-fathom`)

Introducing Fathom Lite - the popular, open-source website analytics tool with millions of downloads! Long-term maintenance, bug fixes, and cookie-free tracking set it apart. Get started today!

[Service documentation](https://docs.osaas.io/osaas.wiki/Service%3A-Fathom-Lite.html) · [Source](https://github.com/usefathom/fathom)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "usefathom-fathom"
  name       = "myfathom"

  parameters = {
    AdminEmail    = "..."
    AdminPassword = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `AdminEmail` | string | yes | Configuration option for adminemail |
| `AdminPassword` | string | yes | Configuration option for adminpassword |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
