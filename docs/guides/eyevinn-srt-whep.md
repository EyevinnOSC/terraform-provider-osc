---
page_title: "eyevinn-srt-whep: SRT WHEP Bridge"
subcategory: "Services"
description: |-
  SRT to WHEP application ingests MPEG-TS over SRT stream and outputs to WebRTC using WHEP signaling protocol, supporting MacOS and Ubuntu.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# SRT WHEP Bridge (`eyevinn-srt-whep`)

SRT to WHEP application ingests MPEG-TS over SRT stream and outputs to WebRTC using WHEP signaling protocol, supporting MacOS and Ubuntu. No video transcoding, SDP offer/answer exchange focus, and compliance with popular production software. Get yours now!

[Service documentation](https://docs.osaas.io/osaas.wiki/Service%3A-SRT-WHEP-Bridge.html) · [Source](https://github.com/Eyevinn/srt-whep)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "eyevinn-srt-whep"
  name       = "mywhep"

  parameters = {
    SourceIp   = "..."
    SourcePort = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `SourceIp` | string | yes |  |
| `SourcePort` | string | yes |  |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
