---
page_title: "postgrest-postgrest: PostgREST"
subcategory: "Services"
description: |-
  Transform your PostgreSQL database into a high-performance RESTful API with PostgREST.
---
<!-- generated by catalogsync from the OSC catalog; do not edit by hand -->

# PostgREST (`postgrest-postgrest`)

Transform your PostgreSQL database into a high-performance RESTful API with PostgREST. Enjoy rapid response times, enhanced security, and seamless scaling for robust, efficient app development.

[Service documentation](https://docs.osaas.io/osaas.wiki/Service%3A-PostgREST.html) · [Source](https://github.com/PostgREST/postgrest)

## Example

```terraform
resource "osc_instance" "example" {
  service_id = "postgrest-postgrest"
  name       = "mypostgrest"

  parameters = {
    DbUri      = "..."
    # DbAnonRole = "..."
    # DbSchemas  = "..."
  }
}
```

Commented lines are optional parameters. Values marked `"..."` need a real value. Outputs are `url`, `external_ip`, `external_port` and the raw `instance` document, see the `osc_instance` resource.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `DbUri` | string | yes | Database connection configuration |
| `DbAnonRole` | string | no | Database connection configuration |
| `DbSchemas` | string | no | Database connection configuration |

The instance `name` must match `^\w+$` and be unique per service within the workspace. OSC lowercases it and allows at most 20 characters.

Parameter values are strings; booleans take `"true"` or `"false"`. Reference an `osc_secret` as `osc_secret.<name>.ref` to keep a value out of the instance configuration. This page is generated from the OSC catalog; the provider validates against the live catalog when planning.
