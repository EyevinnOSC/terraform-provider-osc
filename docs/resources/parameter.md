---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "osc_parameter Resource - osc"
subcategory: ""
description: |-
  Manages one value in a parameter store. Every value in the store a My App is bound to becomes an environment variable of the app, named by key.
  Set exactly one of value and secret_value. A secret value is encrypted at rest, masked everywhere but in the bound app, and hidden from plan output; it needs a store with enable_secrets. A value that has been stored as a secret cannot be turned back into a plain value in place.
  Apps read their environment when they start, so a changed value reaches a running app on its next restart. To restart it from Terraform, include the values in the app's rebuild_trigger.
---

# osc_parameter (Resource)

Manages one value in a parameter store. Every value in the store a My App is bound to becomes an environment variable of the app, named by `key`.

Set exactly one of `value` and `secret_value`. A secret value is encrypted at rest, masked everywhere but in the bound app, and hidden from plan output; it needs a store with `enable_secrets`. A value that has been stored as a secret cannot be turned back into a plain value in place.

Apps read their environment when they start, so a changed value reaches a running app on its next restart. To restart it from Terraform, include the values in the app's `rebuild_trigger`.

## Example Usage

```terraform
# Each value becomes an environment variable of every app bound to the store.
resource "osc_parameter" "node_env" {
  parameter_store = osc_parameter_store.api.name
  key             = "NODE_ENV"
  value           = "production"
}

# Secret values are encrypted at rest and hidden from plan output.
resource "osc_parameter" "database_url" {
  parameter_store = osc_parameter_store.api.name
  key             = "DATABASE_URL"
  secret_value    = "postgres://api:${var.db_password}@${var.db_host}:5432/api"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `key` (String) The key, which is also the environment variable name in a bound app, e.g. `DATABASE_URL`. Changing it replaces the value.
- `parameter_store` (String) Name of the parameter store, e.g. `osc_parameter_store.<name>.name`. Changing it replaces the value.

### Optional

- `secret_value` (String, Sensitive) A secret value, encrypted at rest.
- `value` (String) A plain value, readable by anyone with access to the store.

### Read-Only

- `id` (String) Identifier in the form `parameter_store/key`. Use it with `terraform import`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# <parameter_store>/<key>
terraform import osc_parameter.node_env myapiconfig/NODE_ENV
```
